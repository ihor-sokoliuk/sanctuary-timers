# Validation record

Implementation is in progress. No native application has been launched yet.

Tests are written before the corresponding behavior and run against missing/stubbed implementations before implementation. Live verification must not synthesize mouse/keyboard input, foreground windows, or interrupt Diablo.

Planned observations: full unit suite, live feed adaptation, persisted settings/cache, startup registration, single instance, actual window styles/bounds, foreground preservation, request counts, CPU time, private memory, handle stability and passive overlay visibility. User-driven dragging, collapse, settings, Alt+Tab and monitor moves require separate manual confirmation if not naturally exercised during play.
